# מדריך תפעול קצר

## הוספת פודקאסט חדש או מקור נוסף

1. פותחים את טופס ההצטרפות:

   ```text
   https://shaqo88.github.io/youtube-podcast-feeds/onboard/
   ```

2. בוחרים איפה נמצאים השיעורים:

   - יוטיוב
   - תיקיית Google Drive
   - פיד פודקאסט קיים

   בשדה יוטיוב אפשר להדביק ערוץ או פלייליסט. אם הקישור הוא פלייליסט,
   המערכת תזהה זאת לבד. בטופס הציבורי בוחרים מקור אחד בכל בקשה; מקור נוסף
   לאותו פודקאסט אפשר להוסיף בבקשה נוספת עם אותו שם קצר.

3. ממלאים את הפרטים. ביוטיוב ו-Drive השדה `שם קצר לקישור באנגלית` חובה,
   לדוגמה:

   ```text
   rav-example
   ```

4. בפיד קיים מדביקים רק את קישור הפיד; המערכת תיצור שם קצר מתוך נתוני
   הפיד.
5. אם זה פודקאסט חדש, משתמשים בשם קצר חדש.
6. אם רוצים להוסיף מקור לפודקאסט קיים, משתמשים באותו שם קצר שכבר קיים.
   אחרי אישור, המערכת תוסיף רק את המקור החדש ולא תיצור פודקאסט נוסף.
7. אחרי השליחה נוצר Issue בגיטהאב עם התווית `needs-approval`.

## אישור פודקאסט מתיקיית Drive

1. מריצים:

   ```text
   Actions -> Check Drive Folder -> Run workflow
   ```

2. מדביקים את קישור התיקייה ובודקים שיש לפחות קובץ אחד לפרסום.
3. אם הכל תקין, מוסיפים ל-Issue את התווית:

   ```text
   approved
   ```

4. GitHub Actions יוצר את הפודקאסט, מסנכרן פרקים ראשונים, מפרסם Feed,
   מוסיף תגובה עם הקישור, מסיר `needs-approval`, וסוגר את ה-Issue.

## אישור פודקאסט מיוטיוב

1. פותחים את קישור היוטיוב ובודקים שזה הערוץ או הפלייליסט הנכון.
2. אם הכל תקין, מוסיפים ל-Issue את התווית:

   ```text
   approved
   ```

3. המערכת תנסה לזהות את מזהה הערוץ או הפלייליסט, לקחת תמונה מיוטיוב אם לא
   סופקה תמונה, ליצור או לעדכן Config, לסנכרן פרקים ראשונים, ולפרסם Feed.

## אישור פודקאסט משולב יוטיוב + Drive

1. הטופס הציבורי כבר לא יוצר בקשה משולבת.
2. מאשרים קודם מקור אחד לפודקאסט.
3. כדי להוסיף מקור נוסף, מגישים בקשה חדשה עם אותו `שם קצר לקישור באנגלית`.
4. אחרי אישור המקור החדש, שני המקורות יסונכרנו לאותו Feed.

## אישור פודקאסט מפיד קיים

1. פותחים את קישור הפיד ובודקים שהוא RSS או Atom תקין עם קבצי אודיו.
2. אם זה מקור נוסף לפודקאסט קיים, משתמשים באותו שם קצר באנגלית.
3. אם הכל תקין, מוסיפים ל-Issue את התווית:

   ```text
   approved
   ```

4. לפודקאסט חדש, המערכת תיקח מהפיד את שם הפודקאסט, הרב/מחבר, התיאור,
   קישור האתר, התמונה והפרקים.
5. כברירת מחדל לפיד קיים, המערכת תשתמש בקישורי האודיו המקוריים מתוך הפיד
   ולא תעתיק את כל קבצי האודיו ל-R2.
6. אם צריך עותק עצמאי של Torah Pod, אפשר להגדיר ידנית `delivery_mode: mirror`
   ואז המערכת תעתיק ותנרמל את הקבצים ל-R2.

## הוספת מקור לפודקאסט קיים

1. בטופס משתמשים באותו `שם קצר לקישור באנגלית` של הפודקאסט הקיים.
2. בוחרים את המקור החדש.
3. אחרי אישור, המערכת מוסיפה ל-Config רק מקורות שלא קיימים עדיין.
4. הפרקים החדשים והישנים מופיעים יחד באותו RSS, ממוינים לפי תאריך.

## מחיקת פודקאסט ניסיון

1. מוחקים את התיקיות:

   ```text
   shows/<slug>
   public/<slug>
   ```

2. דוחפים את השינוי ל-`main`.
3. מוחקים את קבצי האודיו מ-R2:

   ```text
   Actions -> Delete R2 Prefix -> Run workflow
   ```

4. מכניסים:

   ```text
   prefix=<slug>
   confirm=DELETE
   ```

## Nachmanson

הפודקאסט של הרב נחמנסון כבר הועבר לריפו הזה עם הפרקים, התמונה, הפלייליסט,
והנתיב הסטנדרטי ב-R2:

```text
https://shaqo88.github.io/youtube-podcast-feeds/nachmanson/feed.xml
```

הפיד הישן נשאר זמין כדי להפנות אפליקציות לפיד החדש באמצעות
`itunes:new-feed-url`:

```text
https://shaqo88.github.io/enachmanson-feed/feed.xml
```

לא צריך להריץ יותר מיגרציה של R2 או להוסיף את מזהה הפלייליסט; זה כבר בוצע.
